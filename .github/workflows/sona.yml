name: SonarCloud Scan
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
    
jobs:
  sona:
    runs-on: windows-latest
    steps:
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v1.3
